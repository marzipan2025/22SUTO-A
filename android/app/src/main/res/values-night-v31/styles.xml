<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 리소스 우선순위상 night 한정자가 버전(v31) 한정자보다 앞선다.
         이 파일이 없으면 다크모드 기기에서 values-night 가 values-v31 을 이겨서
         위의 스플래시 설정이 적용되지 않는다. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_none</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/splash_bg</item>
    </style>
</resources>
